# Task handoff

Snapshot cutoff: 2026-08-31 16:13:37 CST (UTC+08:00)
Freshness: Day 9 代码、提交、13 个解析产物、全项目测试、Ruff、语料格式和 Git 状态均在本快照前现场核验；动态状态在该时间后可能变化。

## Objective and acceptance criteria

- Original objective: 检查第九天学习任务及完成情况，整理交接，并为明天开始 Day 10“PPT 与 Word 解析”做好准备。
- Day 9 success criteria（课程原文）: 产出 PDF 解析脚本与抽样结果；提取正文、页码、标题和段落，识别扫描版、双栏、公式、表格等风险；抽查页码和正文对应正确，乱码可识别。
- Day 10 success criteria（课程原文）: 提取幻灯片标题、正文、备注及 Word 标题层级，统一成标准文档结构；产出 PPT/Word 解析模块；验收不同格式能输出统一字段。
- Out of scope / authorization limits: 本次只盘点和交接，不实现 Day 10、不下载外部语料、不安装依赖、不创建新提交。后续继续保持“讲解—用户动手—检查纠正”的学习节奏，不直接代做全天任务。

## Current state

- Status: Day 9 completed（按首版工程验收；文档标题来自 registry，layout block 不等于 semantic paragraph）；Day 10 not started，范围和首要准备缺口已确认。
- Last safe checkpoint: `main`/`origin/main` 均位于 `87b39c3 feat: add day 9 PDF parsing pipeline`；Day 9 的 52 项回归测试和 Ruff 均通过。
- Immediate next action: Day 10 开始时先确认或补充至少 1 份真实 `.pptx` 和 1 份真实 `.docx` 样本，并登记进 registry；当前 13 份原始语料全部是 PDF，不能用 `SHINE加速器控制系统设计与开发.pdf` 代替 PPTX 解析验收。

## Facts, decisions, and constraints

- [Fact] 计划中的 Day 9 主题是 PDF 文档解析，Day 10 主题是 PPT 与 Word 解析。— Evidence: `加速器RAG与向量库_30天学习计划_纯计划版 (1).pdf` 第 4 页逐日计划表。
- [Fact] Day 9 已形成 parser、批处理脚本、测试、报告和 13 个可重建的 JSON 解析产物。— Evidence: `src/accelerator_rag/corpus/pdf_parser.py`、`scripts/day9_parse_pdfs.py`、`tests/corpus/test_pdf_parser.py`、`reports/day9_pdf_parsing.md`、`data/parsed/pdf/*.json`。
- [Fact] 当前解析产物统计为 13 文件、2178 页、3,817,630 字符、38,365 blocks、1,147 图片；13 个 JSON 的 schema、doc_id、parser 标识和页数一致性检查均通过。
- [Fact] 风险统计为 `empty_text=7`、`possible_garbled_text=43`、`private_use_characters=64`、`possible_two_column=18`、`possible_scanned_page=2`；warning 是风险提示，不等于已确认错误。
- [Fact] 报告记录了 White Rabbit 双栏论文、SHINE 展示型 PDF、中文加速器分册三类人工抽样，并区分了私用字符与真实乱码。
- [Fact] Day 9 没有自动重建跨 block 标题或自然语言段落；artifact 通过 registry 保留文档标题，并保留原始 text block/bbox 作为后续结构恢复证据。— Evidence: `reports/day9_pdf_parsing.md` 第 6、17 节。
- [Decision] Day 9 只保存物理 PDF 页码和原始布局证据，不在 parser 内做 OCR、清洗、断词修复、表格重建、Chunk 或 Embedding。— Reason: 与 Day 11–14 的职责边界保持一致。
- [Decision] `data/parsed/pdf/*.json` 是可重建中间产物并被 Git 忽略；registry 是人工维护的 source of truth。
- [Constraint] 现有 registry 的 13 条记录均为 `document_type=pdf`，项目内没有 `.ppt/.pptx/.doc/.docx` 文件；`pyproject.toml` 也尚未声明 PPTX/DOCX 解析依赖。

## Workspace and artifacts

- Environment / CWD / repository: `/home/whloong/projects/accelerator-rag`，Git 仓库；运行验证使用 Conda 环境 `accelerator-rag`。
- Branch, commit, and worktree state: `main` 与 `origin/main` 同步于 `87b39c35ffd36b49bcffe204d85d344e49a054bb`；快照前唯一未跟踪项为已有 `handoffs/` 目录，本交接文件也位于其中。
- User-owned pre-existing changes: `handoffs/TASK_HANDOFF_2026-08-27_16-02-52_UTC+0800.md` 在本次开始前已存在且未跟踪，未修改。
- `src/accelerator_rag/corpus/pdf_parser.py` — artifact state: read only（已提交）— PDF 页级解析、block/bbox 与风险识别 — verification state: verified。
- `scripts/day9_parse_pdfs.py` — artifact state: read only（已提交）— registry 驱动的批量解析与原子写入 — verification state: verified。
- `reports/day9_pdf_parsing.md` — artifact state: read only（已提交）— 真实语料统计、抽样和局限 — verification state: verified against current artifacts。
- `data/parsed/pdf/*.json` — artifact state: read only / ignored derived artifacts — 13 份结构化解析结果 — verification state: verified。
- `handoffs/TASK_HANDOFF_2026-08-31_16-13-37_UTC+0800.md` — artifact state: created — Day 9→Day 10 续接快照 — verification state: verified after write。

## Commands and verification

| Check or action | Result | Meaning | Verified at |
|---|---|---|---|
| `conda run -n accelerator-rag pytest -q` | passed: 52 tests | Day 9 功能及全项目回归通过 | 2026-08-31 16:11 CST |
| `conda run -n accelerator-rag ruff check .` | passed | 当前代码静态检查通过 | 2026-08-31 16:11 CST |
| Python 只读校验 13 个 parsed JSON | passed: `invalid=[]` | 结构、文档标识、parser 名称与页数一致，汇总与报告相符 | 2026-08-31 16:12 CST |
| `find` 检查原始与办公文档格式 | passed: 13 PDF；0 PPT/PPTX/DOC/DOCX | Day 10 缺少真实两类输入样本 | 2026-08-31 16:12 CST |
| `git status --short --branch` | passed: branch synced；仅 `?? handoffs/` | Day 9 已提交推送；交接目录未跟踪 | 2026-08-31 16:12 CST |
| `jq` JSON 校验尝试 | attempted: command not found | 输出中的 `INVALID` 由缺少 `jq` 导致，不代表 JSON 无效；随后已用 Python 成功复核 | 2026-08-31 16:12 CST |

## Uncertainties, blockers, and risks

- [Needs confirmation] Day 10 使用哪些真实 PPTX/DOCX：优先选择与加速器/EPICS 相关、允许保存在项目中的小型样本；若暂时没有，可先创建最小测试 fixture 学结构，但它不能替代真实格式抽样验收。
- [Needs confirmation] 统一结构的具体 schema 应在看过两类样本后定稿；建议共同字段至少覆盖 `doc_id`、`source_path`、`document_type`、顺序索引、`title`、`text`、`metadata`、`warnings`，PPT 备注与 Word 标题层级保留为格式特有字段。
- [Risk] `.ppt/.doc` 旧二进制格式与 `.pptx/.docx` OOXML 格式需要不同工具；Day 10 首版建议明确只支持 `.pptx/.docx`，旧格式是否纳入需用户确认。
- [Risk] 幻灯片中的组合图形、表格、图表及阅读顺序，Word 中的列表、表格、页眉页脚和分页信息，不能仅凭“成功取到文本”判定通过。
- [Tooling note] 当前 shell 解析到的 bundled `pdfinfo` 因 GLIBC 版本不兼容失败，但系统 `pdftotext` 成功提取了计划文本；这不影响 Day 9 Python parser 验证。
- Sensitive values omitted: 无凭据或秘密被记录。

## Remaining work

1. Day 10 开始前重新检查 Git 状态，并读取本交接、课程计划 Day 10 行、Day 9 的统一 artifact 结构。
2. 让用户确认/放入 1 个 `.pptx` 和 1 个 `.docx` 真实样本；更新 registry schema 对这两种类型的支持与登记信息。
3. 先共同设计跨格式统一模型，再分别实现 PPTX（标题、正文、备注、顺序）和 DOCX（标题层级、正文、顺序）的最小解析器。
4. 为格式校验、顺序、空内容、备注、标题层级与统一字段写测试；对真实样本做视觉/结构抽查。
5. 形成 Day 10 报告，运行全项目 pytest 与 Ruff；是否创建提交由用户明确决定。
- Completion evidence still required: 两种真实输入格式、统一字段输出示例、PPT 备注抽查、Word 标题层级抽查、相关单元测试及全项目回归结果。

## Starter prompt for the new conversation

请读取 `/home/whloong/projects/accelerator-rag/handoffs/TASK_HANDOFF_2026-08-31_16-13-37_UTC+0800.md`，先核验当前 Git 状态、Day 9 关键文件和输入语料格式。我要以教师引导方式开始 Day 10“PPT 与 Word 解析”：目标是提取幻灯片标题、正文、备注及 Word 标题层级，统一为标准文档结构，并以不同格式输出统一字段作为验收。请保留现有用户改动，不把 changed/attempted/not-run 当成 verified，也不要提前开始 Day 11 清洗、Chunk、Embedding 或 Qdrant。第一步先确认真实 `.pptx`/`.docx` 样本来源；目前项目内只有 13 份 PDF，若无真实样本，可先用最小 fixture 教我理解结构，但不能把 fixture 当成最终真实语料验收。

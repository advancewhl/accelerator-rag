# Task handoff

Snapshot cutoff: 2026-08-27 16:02:52 CST (UTC+08:00)
Freshness: 仓库分支、提交、工作区状态、13 份 PDF 和 Day 8 关键产物在快照前已现场核验；测试结果来自 Day 8 提交后的完整检查，未为本次 handoff 重跑。

## Objective and acceptance criteria

- Original objective: 按《加速器 RAG 与向量库 - 30 天学习计划》推进教学项目；Day 8 已结束，下一窗口开始 Day 9“PDF 文档解析”。
- Day 9 success criteria: 形成 PDF 解析脚本与抽样结果；能提取正文、页码、标题和段落；抽查页码与正文对应正确，并能识别扫描版、双栏、公式、表格和乱码风险。
- Out of scope / authorization limits: 不要提前 Embedding、切块或写入 Qdrant；不要未经用户明确要求代做完全天任务或创建 Git commit。下一阶段应以讲解、提问、用户动手、再验证的节奏进行。

## Current state

- Status: Day 8 completed；Day 9 not started。
- Last safe checkpoint: `main` 位于提交 `fd3678d feat: add day 8 corpus registry and inventory`，工作区干净。
- Immediate next action: 先向用户讲清 Day 9 的解析目标、最小统一输出结构和抽样策略，再让用户选择 1 份简单英文 PDF 亲手完成首次按页提取；不要直接批量处理 13 份。

## Facts, decisions, and constraints

- [Fact] `data/raw` 现有 13 份真实 PDF，已全部登记。— Evidence: 快照前 `find data/raw -type f -name '*.pdf'` 返回 13 项。
- [Fact] Day 8 inventory 为 13 registered / 13 raw / 69.7 MiB，分类为 control_system=8、accelerator_physics=3、beam_diagnostics=2。— Evidence: `reports/day8_corpus_inventory.md` 与此前 inventory 输出。
- [Decision] registry 使用一个主 `category` + 一个主 `topic`；跨主题多标签留到 Day 13。— Reason: 避免 Day 8 过早复杂化和重复统计。
- [Decision] 状态区分 `active`、`reference`、`draft`、`deprecated`、`unknown`；旧但仍有价值的论文标为 `reference`，不是 `deprecated`。
- [Decision] `doc_id` 表示稳定身份，文件移动只修改 `source_path`。
- [Constraint] 用户明确纠正过协作方式：“教我完成”不等于代理完成。后续必须在关键节点停下来让用户回答或编码。
- [Fact] 用户已理解集合差：`registered - raw` 找登记但缺失，`raw - registered` 找存在但未登记；理解文件移动时 `doc_id` 不变、`source_path` 改变。

## Workspace and artifacts

- Environment / CWD / repository: `/home/whloong/projects/accelerator-rag`，Git 仓库，Conda 环境名 `accelerator-rag`。
- Branch, commit, and worktree state: `main`，HEAD/`origin/main` 均为 `fd3678d`；快照时 `git status --short` 无输出。
- User-owned pre-existing changes: 无未提交变更。
- `data/documents/registry.json` — artifact state: created — 13 份文档登记表 — verification state: verified。
- `src/accelerator_rag/corpus/registry.py`、`taxonomy.py` — artifact state: created — schema、分类与 inventory 校验 — verification state: verified。
- `scripts/day8_corpus_inventory.py` — artifact state: created — 文件清单、一致性检查和统计 — verification state: verified。
- `reports/day8_corpus_inventory.md` — artifact state: created — Day 8 报告 — verification state: verified。
- `加速器RAG与向量库_30天学习计划_纯计划版 (1).pdf` — artifact state: read only / committed — 课程计划来源 — verification state: verified readable（9 页）。

## Commands and verification

| Check or action | Result | Meaning | Verified at |
|---|---|---|---|
| `conda run -n accelerator-rag python scripts/day8_corpus_inventory.py` | passed | 13/13 一致，`validation: OK` | Day 8 提交后，time not recorded |
| `conda run -n accelerator-rag pytest -q` | passed: 43 tests | 全项目回归通过 | Day 8 提交后，time not recorded |
| `conda run -n accelerator-rag ruff check .` | passed | 静态检查通过 | Day 8 提交后，time not recorded |
| `git status --short` | passed: empty | 当前工作区干净 | 2026-08-27 16:02 CST |

## Uncertainties, blockers, and risks

- [Needs confirmation] Day 9 首个教学样本建议选 3 页英文论文 `Standard_E-Beam_Diagnostics_for_European_XFEL.pdf`，但应让用户决定或确认。
- [Risk] 不同 PDF 的双栏、扫描页、公式和表格会导致文本顺序或字符错误；不能用“能提取出文本”代替抽样视觉核对。
- [Risk] Day 9 不应顺带开始 chunk、Embedding 或 Qdrant ingestion。

## Remaining work

1. 讲解解析输出的最小数据结构，例如 `doc_id`、`page_number`、`title`、`text`、`extraction_method`、`warnings`。
2. 用户亲手检查一个简单 PDF 的页数、文本层和第一页提取结果。
3. 在指导下实现最小按页解析函数，并写针对页码与空页的测试。
4. 用简单英文 PDF、中文 PDF、双栏 PDF 各抽样验证，记录风险；完成 Day 9 报告后再运行 pytest/ruff，是否 commit 由用户明确决定。
- Completion evidence still required: Day 9 脚本、抽样输出、页码对应检查、风险记录、测试与静态检查结果。

## Starter prompt for the new conversation

请读取 `/home/whloong/projects/accelerator-rag/handoffs/TASK_HANDOFF_2026-08-27_16-02-52_UTC+0800.md`，并先核验当前仓库状态。我要开始 Day 9“PDF 文档解析”，验收目标是提取正文、页码、标题和段落，抽查页码与正文对应，并识别扫描版、双栏、公式、表格和乱码风险。请以教师方式推进：先讲概念并提问，让我亲手完成小步骤，再检查和纠正；不要直接代做完整 Day 9，不要提前切块、Embedding、写 Qdrant 或提交 Git。先从解析输出结构和第一份简单样本文档的选择开始。

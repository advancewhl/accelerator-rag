# Task handoff

Snapshot cutoff: 2026-09-04 14:04:51 CST (UTC+08:00)
Freshness: 5 份真实 PPTX/DOCX 样本已下载并登记、inventory 校验、Git 状态和计划原文均在本快照前现场核验；动态状态在该时间后可能变化。

## Objective and acceptance criteria

- Original objective（本次会话）: 按第十天学习计划，下载几个合适的 PPT 和 Word 资料，放到与目录结构一致的语料位置。
- Day 10 success criteria（课程原文）: 提取幻灯片标题、正文、备注及 Word 标题层级；统一成标准文档结构；验收不同格式能输出统一字段。
- 本次完成范围: 只补样本与登记，不实现 Day 10 解析器、不安装依赖、不创建新提交；保留“讲解—用户动手—检查纠正”的学习节奏。

## Current state

- Status: Day 9 completed；Day 10 的准备缺口（真实 `.pptx/.docx` 样本）本次已补齐并登记；Day 10 解析器尚未开始。
- Corpus: 原有 13 份 PDF + 新增 5 份 Office 样本 = 18 份注册文档；`data/raw` 实际文件数 18，inventory 校验通过。
- Last safe checkpoint: `main`/`origin/main` 均位于 `87b39c3 feat: add day 9 PDF parsing pipeline`；本次仅修改 `data/documents/registry.json`，`handoffs/` 未跟踪。
- Immediate next action: 读取本交接、课程计划 Day 10 行和 Day 9 统一 artifact 结构，然后共同设计跨格式 schema 并实现最小 PPTX/DOCX 解析器。

## Facts, decisions, and constraints

- [Fact] Day 10 主题是 PPT 与 Word 解析，目标为“提取幻灯片标题、正文、备注及 Word 标题层级；统一成标准文档结构”。— Evidence: `加速器RAG与向量库_30天学习计划_纯计划版 (1).pdf` 第 4 页逐日计划表（Day 10，7/22）。
- [Fact] 交接前项目内 13 份原始语料全部为 PDF，缺少真实 `.pptx/.docx`；registry 代码已允许 `document_type=pptx/docx`，但尚无此类样本。— Evidence: `TASK_HANDOFF_2026-08-31_16-13-37_UTC+0800.md`、`data/documents/registry.json`、`src/accelerator_rag/corpus/registry.py`。
- [Fact] 本次新增样本均为公开可下载的真实 Office 文档；来源为 GitHub `epics-training` 组织、INFN-LNF 公开 Confluence 附件、EPICS Tech-talk 邮件列表附件。
- [Decision] 新样本统一放入 `data/raw/control_system/`（该目录在目录结构中已存在但为空），不再新建顶层目录；registry 中 category/topic 按内容归类。
- [Decision] 样本侧重 Day 10 验收所需结构：PPT 覆盖不同页数与备注情况，Word 覆盖有 Heading 样式与无 Heading 样式的两种真实情况。
- [Constraint] `data/raw/*` 被 `.gitignore` 排除，样本文件不会进 Git；只有 registry 变更会进入提交，适合“样本只在本机、元数据入库”的现状。
- [Constraint] `pyproject.toml` 尚未声明 PPTX/DOCX 解析依赖（如 python-pptx、python-docx）；Day 10 实现前需确认安装方案。
- [Constraint] 计划验收要求真实格式抽样；最小 fixture 只能用于学结构，不能替代真实样本验收。

## Workspace and artifacts

- Environment / CWD / repository: `/home/whloong/projects/accelerator-rag`，Git 仓库；运行验证使用 Conda 环境 `accelerator-rag`。
- Branch, commit, and worktree state: `main` 与 `origin/main` 同步于 `87b39c35ffd36b49bcffe204d85d344e49a054bb`；未跟踪项为 `handoffs/`。
- New raw samples（Git 忽略，仅本机可用）:
  - `data/raw/control_system/EPICS_7_Overview.pptx` — 14 页、4 个 notesSlide；EPICS 7 概述；来源 `github.com/epics-training/epics7-overview`；registry 版本记录为 modified 2025-04-10。
  - `data/raw/control_system/EPICS_IOC_Setup.pptx` — 9 页、3 个 notesSlide；IOC 搭建（Timo Korhonen）；来源 `github.com/epics-training/stream-introduction`。
  - `data/raw/control_system/areaDetector_training.pptx` — 60 页、60 个 notesSlide（多数备注为占位或空，适合测试“有/无/空备注”分支）；来源 `github.com/epics-training/areadetector-training`。
  - `data/raw/control_system/LNL_Accelerator_Complex_Code_Legend_Rule.docx` — 真实 Word；含 Heading1/Heading3 样式；INFN-LNF 加速器命名规范；registry 版本记录为 Version 5 (2023-05-19)。
  - `data/raw/control_system/EPICS_Centos8_Packages.docx` — 真实 Word；段落正文为主、无 Heading 样式（可测试“按样式 vs 按视觉粗体”的边界）；EPICS Tech-talk 附件；Mark Rivers, 2020-04-02。
- `data/documents/registry.json` — artifact state: modified — 新增 5 条记录，字段齐全（doc_id/title/source_path/category/topic/document_type/language/version/status）。
- 未改动 Day 9 代码、解析产物、报告；未实现任何 Day 10 代码。

## Commands and verification

| Check or action | Result | Meaning | Verified at |
|---|---|---|---|
| `pdftotext` 提取 30 天计划第 4 页 | Day 10 行已定位 | 确认 Day 10 目标和验收口径 | 2026-09-04 |
| 下载并 `file` 校验 5 份样本 | 3×Microsoft PowerPoint 2007+、2×Microsoft Word 2007+ | 均为真实 OOXML 文件，非 HTML 假文件 | 2026-09-04 |
| zip 结构抽查（slides/notesSlide、Heading 样式计数） | PPTX 分别 14/9/60 页；Word 一份含 Heading1+Heading3 | 样本覆盖 PPT 备注与 Word 标题层级两类结构 | 2026-09-04 |
| `conda run -n accelerator-rag python scripts/day8_corpus_inventory.py` | passed: validation OK；registered=18、raw_files=18、docx=2、pptx=3 | registry 与 data/raw 一一对应、无缺失无漏登记 | 2026-09-04 13:5x CST |
| `git status --short --branch` | `M data/documents/registry.json`、`?? handoffs/` | 代码基线未变，仅 registry 与 handoff 目录有改动 | 2026-09-04 14:04 CST |

## Uncertainties, blockers, and risks

- [Needs confirmation] 三份 PPTX 的 speaker notes 内容大多为空或占位（如页码数字），只有少数页有真实备注文本；解析验收时应把“空备注”当作合法输出，而不是解析失败。
- [Needs confirmation] Word 中“没有 Heading 样式、仅靠加粗/字号表达标题”的文档如何归类：建议 Day 10 首版严格以 OOXML 样式为依据，视觉推断留给后续清洗或人工标记。
- [Needs confirmation] 统一结构的具体 schema 应在看过 5 份样本后定稿；建议共同字段覆盖 `doc_id`、`source_path`、`document_type`、顺序索引、`title`、`text`、`metadata`、`warnings`，PPT 备注与 Word 标题层级保留为格式特有字段。
- [Risk] `.pptx/.docx` 中图形化文本、SmartArt、文本框阅读顺序、表格与页眉页脚是否纳入首版解析范围，需与用户明确边界，避免“能打开文件”被误当作“解析正确”。
- [Risk] 外部样本链接可能失效或内容变动；样本文件已在本地 `data/raw/control_system/` 留存，registry 元数据已固化，后续以本地文件为准。
- Sensitive values omitted: 无凭据或秘密被记录。

## Remaining work

1. 让用户/新会话读取课程计划 Day 10 行、本交接和 Day 9 的 artifact/schema 习惯，确认 5 份新样本的实际版式。
2. 先共同设计跨格式统一模型，再分别实现最小 `.pptx`（标题、正文、备注、顺序）与 `.docx`（标题层级、正文、顺序）解析器；依赖安装方案由用户确认。
3. 为格式识别、顺序、空内容、备注、标题层级、统一字段写测试；对真实样本做视觉/结构抽查。
4. 形成 Day 10 报告；运行全项目 pytest 与 Ruff；是否创建提交由用户明确决定。
- Completion evidence still required: 两种真实输入格式的统一字段输出示例、PPT 备注抽查、Word 标题层级抽查、相关单元测试及全项目回归结果。

## Starter prompt for the new conversation

请先读取 `/home/whloong/projects/accelerator-rag/handoffs/TASK_HANDOFF_2026-09-04_14-04-51_UTC+0800.md`，再核验 `data/raw/control_system/` 下 5 份新增真实样本、`data/documents/registry.json` 与 `main` 分支 Git 状态。我要以教师引导方式开始 Day 10“PPT 与 Word 解析”：目标是从 `.pptx` 提取幻灯片标题、正文、备注及顺序，从 `.docx` 提取标题层级与正文，并统一成标准文档字段作为验收。请保留用户改动；不要把 changed/attempted/not-run 当作 verified；先与我一起确认统一 schema 和两份 Word 样本（一份有 Heading 样式、一份无 Heading 样式）的归类边界，再实现最小解析器，暂不进入 Day 11 清洗、Chunk、Embedding 或 Qdrant。
